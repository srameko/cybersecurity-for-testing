{
  "name": "czechitas-bezpecnost-101-slides",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /bezpecnost-101/ --out dist",
    "preview": "npm run build && npx serve dist -l 4000"
  },
  "dependencies": {
    "@slidev/cli": "^52.12.0",
    "@slidev/theme-default": "^0.25.0"
  }
}
